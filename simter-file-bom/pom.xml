<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>tech.simter.file</groupId>
    <artifactId>simter-file</artifactId>
    <version>${revision}</version>
    <relativePath>..</relativePath>
  </parent>
  <artifactId>simter-file-bom</artifactId>
  <packaging>pom</packaging>
  <description>BOM</description>
  <developers>
    <developer>
      <name>RJ.Hwang</name>
      <email>rongjihuang@gmail.com</email>
    </developer>
  </developers>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>tech.simter.file</groupId>
        <artifactId>simter-file-core</artifactId>
        <version>${simter-file.version}</version>
      </dependency>
      <dependency>
        <groupId>tech.simter.file</groupId>
        <artifactId>simter-file-dao-jpa</artifactId>
        <version>${simter-file.version}</version>
      </dependency>
      <dependency>
        <groupId>tech.simter.file</groupId>
        <artifactId>simter-file-dao-mongo</artifactId>
        <version>${simter-file.version}</version>
      </dependency>
      <dependency>
        <groupId>tech.simter.file</groupId>
        <artifactId>simter-file-rest-webflux</artifactId>
        <version>${simter-file.version}</version>
      </dependency>
      <dependency>
        <groupId>tech.simter.file</groupId>
        <artifactId>simter-file-starter</artifactId>
        <version>${simter-file.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>flatten-maven-plugin</artifactId>
        <inherited>true</inherited>
        <executions>
          <execution>
            <!-- Flatten and simplify our own POM for install/deploy -->
            <id>flatten</id>
            <phase>process-resources</phase>
            <goals>
              <goal>flatten</goal>
            </goals>
            <configuration>
              <flattenMode>clean</flattenMode>
              <pomElements>
                <parent>resolve</parent>
                <name>keep</name>
                <description>keep</description>
                <dependencyManagement>keep</dependencyManagement>
                <dependencies>keep</dependencies>
                <pluginManagement>keep</pluginManagement>
                <developers>keep</developers>
                <properties>interpolate</properties>
                <profiles>remove</profiles>
              </pomElements>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
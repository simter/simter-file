spring.main.banner-mode: 'off'
simter-file.root-dir: target/files
logging:
  file: target/test.log
  level: # TRACE < DEBUG < INFO < WARN < ERROR < FATAL < OFF
    root: ERROR
    tech.simter.file: WARN